<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:WebshopMobileApp.PageModels"
             xmlns:models="clr-namespace:WebshopMobileApp.Models"
             xmlns:controls="clr-namespace:WebshopMobileApp.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="WebshopMobileApp.Pages.TabbedPages.Catalog"
             x:DataType="pageModels:ProductsListPageModel"
             Title="">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding LoadItemsCommand}" />
    </ContentPage.Behaviors>

    <pullToRefresh:SfPullToRefresh>
        <pullToRefresh:SfPullToRefresh.PullableContent>

            <!--<CollectionView ItemsSource="{Binding Products}"
                            Margin="10">

                <CollectionView.Header>
                    <VerticalStackLayout Spacing="0">

                        <Label Text="{Binding DeliveryDate}"
                           Padding="10"
                           Margin="0,0,0,10"
                           BackgroundColor="#0698d5"
                           FontSize="16"
                           TextColor="White"
                           Style="{StaticResource Title2}"
                           HorizontalTextAlignment="Center" />

                        <VerticalStackLayout Spacing="0"
                             Padding="10"
                             BackgroundColor="#0698d5">

                            <Label Text="Weekly Promotions"
                               Padding="1"
                               FontSize="18"
                               TextColor="White"
                               Style="{StaticResource Title2}"
                               HorizontalTextAlignment="Center" />

                            <ActivityIndicator
                                IsVisible="{Binding ISVisibleSpinner}"
                                IsRunning="True"
                                Color="White" />

                            <ScrollView Orientation="Horizontal"
                        Margin="-30,0">
                                <HorizontalStackLayout
                    Spacing="15"
                    Padding="30,0"
                    BindableLayout.ItemsSource="{Binding Slots}">

                                    <BindableLayout.ItemTemplate>
                                        <DataTemplate x:DataType="models:TblPromoPicturesSet">
                                            <VerticalStackLayout Spacing="6">
                                                <controls:SlotCardView WidthRequest="320" />
                                            </VerticalStackLayout>
                                        </DataTemplate>
                                    </BindableLayout.ItemTemplate>

                                </HorizontalStackLayout>
                            </ScrollView>

                        </VerticalStackLayout>

                    </VerticalStackLayout>
                </CollectionView.Header>


                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical"
                                     Span="3"
                                     HorizontalItemSpacing="10"
                                     VerticalItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:ProductsWithQuantity">
                        <Border Stroke="#000"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 0"
                                BackgroundColor="White"
                                Padding="0">

                            <Image Source="{Binding FileUrl}"
                                    Aspect="AspectFit"
                                    HeightRequest="80"
                                    WidthRequest="100"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center" />


                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>-->
      
            <CollectionView x:Name="ProductsCollectionView"
                    ItemsSource="{Binding Products}"
                    Margin="10">
                <CollectionView.ItemTemplate>
                    <DataTemplate  x:DataType="models:ProductsWithQuantity">
                        <Border Stroke="#f1f1f1" BackgroundColor="blue"
                        StrokeThickness="1" 
                        Padding="10" 
                                StrokeShape="RoundRectangle 18"
                        Margin="5" Background="#fff">
                            <Grid ColumnDefinitions="*,2*" RowDefinitions="Auto,Auto,Auto"  >
                                <!-- Product Image -->
                                <Border Stroke="Black" StrokeThickness="0" Padding="0"  StrokeShape="RoundRectangle 8">
                                    <Image Grid.Column="0"
                                        Source="{Binding FileUrl}"
                                        Aspect="AspectFill"
                                                HeightRequest="120"
                                                BackgroundColor="red"
        
                                        Margin="0"/>

                                </Border>
                               
                                <!-- Product Details -->
                                <StackLayout Grid.Column="1" Margin="10,0,0,0" Spacing="5">
                                    <!-- Title -->
                                    <Label Text="{Binding Description}" FontSize="Small" FontAttributes="Bold"/>
                                    <!-- Product Code -->
                                    <Label Text="{Binding Code}" FontSize="Small" TextColor="Gray"/>

                                    <!-- Price and Availability -->
                                    <Grid ColumnDefinitions="Auto,Auto" RowDefinitions="Auto">
                                        <Label Text="{Binding Price, StringFormat='${0:F2}'}"
                                       FontSize="Medium" TextColor="#0698d5" FontAttributes="Bold"
                                       Grid.Column="0"/>
                                        <Label Text="Available"
                                       FontSize="Small"
                                       TextColor="Green"
                                       Margin="20,0,0,0"
                                       Grid.Column="1"/>
                                    </Grid>

                                    <!-- Quantity and Add Button -->
                                    <Grid ColumnDefinitions="Auto" RowDefinitions="Auto" ColumnSpacing="5">
                                        <!-- Decrement Button -->
                                        <!--<Label Text="_" WidthRequest="20" Margin="0, 1" FontSize="Large" Grid.Column="0"/>
                                        --><!-- Quantity Label -->
                                        <!-- Decrement Button --><!--
                                        <Label Text="{Binding Quantity}" WidthRequest="10" Margin="0, 15" Grid.Column="1"/>-->
                                        
                                        <!--<Label Text="{Binding Quantity}" WidthRequest="10" HorizontalTextAlignment="Center" Grid.Column="1"/>-->
                                        <!-- Increment Button -->
                                        <!--<Label Text="+" WidthRequest="10" Grid.Column="2"/>-->
                                        <!--<Label Text="+" WidthRequest="50" Margin="0, 10" FontSize="Large" Grid.Column="0"/>-->
                                        <!-- Add Button -->
                                        <Button Text="Add" HorizontalOptions="Start" Grid.Column="0" WidthRequest="150" />
                                    </Grid>
                                </StackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </pullToRefresh:SfPullToRefresh.PullableContent>
    </pullToRefresh:SfPullToRefresh>

</ContentPage>
